# Add your own Discord Webhook URL below:
DISCORD_WEBHOOK=https://discord.com/api/webhooks/...

# Optional: override Resy's public web-client API key if it ever changes.
# Leave unset to use the built-in default.
# RESY_API_KEY=

# Optional: HTTP port for the web UI / API (default 8080).
# PORT=8080
